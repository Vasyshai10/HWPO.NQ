// Define the generateText function
function generateText() {
    // Example of generating text (you can replace this with actual logic)
    const generatedContent = "This is the generated text!";

    // Display the generated text in the 'generated-text' div
    document.getElementById("generated-text").innerHTML = generatedContent;

    // For debugging, log the generated text to the console
    console.log(generatedContent);
}
